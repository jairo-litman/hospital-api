{
  "name": "hospital-api",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "express": "^4.21.1",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.7.5",
    "gts": "^6.0.1",
    "jest": "^29.7.0",
    "serverless-offline": "^14.3.3",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "offline": "serverless offline --noSponsor --reloadHandler",
    "test": "jest"
  }
}